# better-sqlite3 has no prebuild for Node 26+, and @electron/rebuild requires >= 22.12.
# Without this, an unsupported Node fails deep inside node-gyp with ~200 lines of C++
# errors that never mention the Node version. Fail fast and legibly instead.
engine-strict=true
